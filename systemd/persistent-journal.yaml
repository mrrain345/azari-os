# Hardcode persistent journal by default. journald has this "auto" behaviour
# that only makes logs persistent if `/var/log/journal` exists, which it won't
# on first boot because `/var` isn't fully populated.

files:
  "/usr/lib/systemd/journald.conf.d/10-persistent.conf":
    format: ini
    content:
      Journal:
        Storage: persistent