files:
  "/usr/lib/ostree/prepare-root.conf":
    format: ini
    content:
      composefs:
        enabled: true
      etc:
        transient: true

  "/usr/lib/bootc/install/00-azari.toml":
    format: toml
    content:
      install.filesystem.root:
        type: btrfs
      install:
        kargs:
          - splash
          - quiet
          - rd.driver.blacklist=nouveau
          - modprobe.blacklist=nouveau
          - rd.driver.blacklist=nova_core
          - modprobe.blacklist=nova_core
          - nvidia-drm.modeset=1
          - drm.edid_firmware=DP-3:edid/fullhd.bin
          - video=DP-3:1920x1080@60e